<!--pages/detail/detail.wxml-->
<view>
    <view wx:for="{{cart}}">
        <view>{{item.name}}</view>
        <view>{{item.price}}</view>
    </view>
    <view>
        {{cart.reduce()}}
    </view>
</view>